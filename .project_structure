# 工程结构

> 自动生成文件，请勿手动编辑。
> 事实来源：`.context/engineering.yaml` + `.workflow/project.yaml`。
> 重新生成：`python tools/project_structure.py generate` 或 `python tools/workflow.py structure`。

## 项目

- 项目：`embedded-ai-workflow-kit`
- 板卡：`unconfigured`
- MCU：`unconfigured`
- 报告目录：`reports`

## 工作流不变目录

以下目录属于可复用的 AI 工作流框架。它们不是架构层目录，在主机操作系统、IDE、编译器、调试器或 AI Agent 更换时保持稳定。

| 路径 | 作用 | 变更时机 | 是否存在 |
| ------ | ------ | ---------- | ---------- |
| `docs` | 工作流文档和交接指南 | 工作流规则、测试清单或使用文档变更时 | 是 |
| `.context` | AI 交接事实源 | 工程、硬件、版本或运行时事实变更时 | 是 |
| `.workflow` | 工作流/项目配置源 | 项目、工具链、布局、测试、烧录或报告配置变更时 | 是 |
| `.agents` | Agent 中立规则和规范 Skill | Agent 规则、Skill 或兼容性元数据变更时 | 是 |
| `tools` | 确定性工作流命令和适配器 | 工作流操作或适配器变更时 | 是 |
| `reports` | 自动化报告的当前证据快照 | 编译、烧录、审查、检查或验证证据刷新时 | 是 |

## 当前项目架构目录

以下目录是该项目声明的架构。它们是可配置的项目事实，而非每个复用了 AI 工作流的项目的必选目录。

| 路径 | 作用 | 变更时机 | 是否存在 |
| ------ | ------ | ---------- | ---------- |

## 平台/工具适配器边界

以下路径在 MCU、板卡、IDE、工具链、生成代码或可选 Agent 适配器变更时可能变化。

| 路径 | 作用 | 变更时机 | 是否存在 |
| ------ | ------ | ---------- | ---------- |

## 当前工具适配器

| 事实 | 值 |
| ------ | ------ |
| 当前工具链 | `none` |
| 编译日志 | `reports/build_log.txt` |
| 烧录日志 | `reports/flash_log.txt` |

## 硬性规则

- 新增分层代码只能放在该项目声明的架构目录中。
- 不得将声明的架构目录移入生成代码/供应商/平台适配器目录。
- 不同项目可以在 `.context/engineering.yaml` 和 `.workflow/project.yaml` 中声明不同的架构目录。
- 操作系统、编译器、IDE、调试器和烧录工具变更属于 `.workflow/project.yaml`、`tools/` 或适配器目录。
- 编译、烧录、检查、审查和验证报告属于 `reports/`，使用固定的覆盖写入路径。
