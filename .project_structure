# 项目目录结构规范（强制）

## 架构分层目录

本工程使用以下目录结构，AI生成代码时必须严格遵守：

```
very_test/
├── App/          # 应用层 - 纯业务逻辑
│   └── *.c, *.h
├── Service/      # 服务层 - 硬件功能抽象
│   └── *.c, *.h
├── Driver/       # 驱动层 - 直接操作寄存器
│   └── *.c, *.h
├── Test/         # 测试代码（持久保留）
│   └── *.c, *.h
├── Core/         # CubeMX 生成代码（纯平台代码）
│   ├── Src/
│   └── Inc/
├── Drivers/      # HAL 库
└── MDK-ARM/      # Keil 工程
```

## 禁止事项

- ❌ 禁止创建 USER 文件夹存放分层代码
- ❌ 禁止创建 Middleware/Application 等其他分层目录
- ❌ 禁止把 App/Service/Driver/Test 放在其他任何位置

## 必须遵守

- ✅ 所有新增模块必须放入 App/、Service/、Driver/ 或 Test/
- ✅ 保持现有文件引用路径不变
- ✅ Keil工程中的Include Path已配置为 Core/Inc、Driver、Service、App

## 违规后果

如果AI把代码生成到USER或其他位置：
1. build/flash skill 将无法找到文件
2. 编译会失败
3. 需要手动迁移代码

## 正确示例

新增LED驱动：
- ✅ Driver/led_driver.c
- ❌ USER/Driver/led_driver.c
- ❌ Core/Driver/led_driver.c  (已迁移到 Driver/led_driver.c)
- ❌ led_driver.c (放在根目录)
