schema: context-hardware-1.0
context_kind: hardware
board:
  name: unconfigured
  notes: No board is bundled with the workflow kit.

mcu:
  family: unconfigured
  device: unconfigured
  core: unconfigured
  clock_hz: null

clocks: {}

resources: []

signals: {}

known_hardware_assumptions:
  - The adopting project must record MCU, clocks, pins, peripherals, resource ownership, and verified hardware behavior here.
